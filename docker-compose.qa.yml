version: '3.9'
name: bis-demo-qa
services:
  keycloak:
    image: quay.io/keycloak/keycloak:24.0.5
    command: ["start-dev", "--http-port=8181", "--import-realm"]
    environment:
      KEYCLOAK_ADMIN: admin
      KEYCLOAK_ADMIN_PASSWORD: admin
    volumes:
      - ./infra/keycloak-realm-bis-demo.json:/opt/keycloak/data/import/realm.json
    ports: ["8181:8181"]

  minio:
    image: minio/minio:RELEASE.2024-01-18T22-51-28Z
    command: server /data --console-address ":9101"
    environment:
      MINIO_ROOT_USER: minioadmin
      MINIO_ROOT_PASSWORD: minioadmin
    ports: ["9100:9000","9101:9101"]
    volumes: [ "minio-data-qa:/data" ]

  clamav:
    image: clamav/clamav:latest
    ports: ["4310:3310"]

  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: bisdb
      POSTGRES_USER: bis
      POSTGRES_PASSWORD: bis
    ports: ["6432:5432"]
    volumes: [ "pg-data-qa:/var/lib/postgresql/data" ]

  backend:
    build: ./backend
    environment:
      SPRING_PROFILES_ACTIVE: postgres
      DB_HOST: postgres
      DB_PORT: 5432
      KEYCLOAK_ISSUER_URI: http://keycloak:8181/realms/bis-demo
      KEYCLOAK_JWK_SET_URI: http://keycloak:8181/realms/bis-demo/protocol/openid-connect/certs
      CORS_ALLOWED_ORIGINS: http://localhost:6173
      S3_ENDPOINT: http://minio:9000
      S3_ACCESS_KEY: minioadmin
      S3_SECRET_KEY: minioadmin
      S3_BUCKET: bis-uploads
      S3_REGION: us-east-1
      S3_PRESIGN_EXPIRY: 900
      CLAMAV_HOST: clamav
      CLAMAV_PORT: 3310
    depends_on: [postgres]
    ports: ["8180:8080"]

  frontend:
    build:
      context: ./frontend
      args:
        VITE_KC_URL: http://localhost:8181
        VITE_KC_REALM: bis-demo
        VITE_KC_CLIENT: bis-frontend
    ports: ["6173:80"]
    depends_on: [backend, keycloak]

volumes:
  minio-data-qa:
  pg-data-qa:
